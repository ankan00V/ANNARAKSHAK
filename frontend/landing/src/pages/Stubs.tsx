import StubPage from '../StubPage'

export function OfficerStub() {
  return (
    <StubPage
      title="Officer Dashboard"
      subtitle="District hotspot map, detection trends, and the expert validation queue."
      note="Officer dashboard is under construction — this page keeps the route alive."
    />
  )
}
